<?xml version="1.0" encoding="utf-8" standalone="yes"?>
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>async on NullDeref</title>
    <link>https://nullderef.com/tags/async/</link>
    <description>Recent content in async on NullDeref</description>
    <image>
      <title>NullDeref</title>
      <url>https://nullderef.com/logo-banner.png</url>
      <link>https://nullderef.com/logo-banner.png</link>
    </image>
    <generator>Hugo -- gohugo.io</generator>
    <language>en</language>
    <copyright>See this site&#39;s source code [here](https://github.com/marioortizmanero/nullderef.com), licensed under GPLv3 ·</copyright>
    <lastBuildDate>Sun, 14 Jan 2024 00:00:00 +0000</lastBuildDate>
    <atom:link href="https://nullderef.com/tags/async/index.xml" rel="self" type="application/rss+xml" />
    <item>
      <title>The bane of my existence: Supporting both async and sync code in Rust</title>
      <link>https://nullderef.com/blog/rust-async-sync/</link>
      <pubDate>Sun, 14 Jan 2024 00:00:00 +0000</pubDate>
      <guid>https://nullderef.com/blog/rust-async-sync/</guid>
      <description>Sit beside me and hear this crazy old man&amp;#39;s tale of when I asked Rust for too much</description>
      <content:encoded><![CDATA[<div class="sect1">
<h2 id="_introduction">Introduction</h2>
<div class="sectionbody">
<div class="paragraph">
<p>Imagine you want to create a new library in Rust. All it does is wrap up a
public API that you need for something else, like
<a href="https://developer.spotify.com/documentation/web-api/">the Spotify API</a> or maybe a
database like <a href="https://www.arangodb.com/">ArangoDB</a>. It’s not rocket science, you
aren’t inventing something new or dealing with complex algorithms, so you expect
it to be relatively straightforward.</p>
</div>
<div class="paragraph">
<p>You decide to implement the library with async. Most of the work in your library
has to do with performing HTTP requests, which are mostly I/O, so it makes sense
(that, and because it’s what the cool kids use in Rust nowadays). You start
coding and have the v0.1.0 release ready in a few days. “Neat”, you say, as
<code>cargo publish</code> finishes successfully and uploads your work to
<a href="https://crates.io">crates.io</a>.</p>
</div>
<div class="paragraph">
<p>A couple of days pass, and you get a new notification on GitHub. Someone opened
an issue:</p>
</div>
<div class="quoteblock">
<blockquote>
<div class="paragraph">
<p><strong>How can I use this library synchronously?</strong></p>
</div>
<div class="paragraph">
<p>My project doesn’t use async because it’s overly complex for what I need. I
wanted to try your new library, but I’m not sure how to do it easily. I would
rather not fill my code with <code>block_on(endpoint())</code>. I’ve seen crates like <a href="https://crates.io/crates/reqwest"><code>reqwest</code></a>
 exporting a
<a href="https://docs.rs/reqwest/0.11.4/reqwest/blocking/index.html"><code>blocking</code> module</a>
with the exact same functionality, could you perhaps do that as well?</p>
</div>
</blockquote>
</div>
<div class="paragraph">
<p>Low-level wise, that sounds like a very complicated task. Having a common
interface for both async code — which requires a runtime like <a href="https://crates.io/crates/tokio"><code>tokio</code></a>
, awaiting futures, pinning, etc — and regular sync code? I mean, they asked
nicely, so maybe we can try. After all, the only difference in the code would be
the occurrences of the <code>async</code> and <code>await</code> keywords because you aren’t doing
anything fancy.</p>
</div>
<div class="paragraph">
<p>Well, this is <em>more or less</em> what happened with the crate <a href="https://crates.io/crates/rspotify"><code>rspotify</code></a>
, which I used to maintain along with its creator
<a href="https://github.com/ramsayleung/">Ramsay</a>. For those who don’t know, it’s a
wrapper for the Spotify Web API. To clarify, I did get this working in the end,
although not as cleanly as I was hoping; I’ll try to explain the situation in
this new article of the <a href="https://nullderef.com/series/rspotify">Rspotify series</a>.</p>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_the_first_approaches">The first approaches</h2>
<div class="sectionbody">
<div class="paragraph">
<p>To give more context, here’s what Rspotify’s client looks like, roughly:</p>
</div>
<div class="paragraph">
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="k">struct</span> <span class="nc">Spotify</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="cm">/* ... */</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">impl</span><span class="w"> </span><span class="n">Spotify</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">async</span><span class="w"> </span><span class="k">fn</span> <span class="nf">some_endpoint</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">param</span>: <span class="nb">String</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nc">SpotifyResult</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">params</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">HashMap</span>::<span class="n">new</span><span class="p">();</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="n">params</span><span class="p">.</span><span class="n">insert</span><span class="p">(</span><span class="s">&#34;param&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">param</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="bp">self</span><span class="p">.</span><span class="n">http</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="s">&#34;/some-endpoint&#34;</span><span class="p">,</span><span class="w"> </span><span class="n">params</span><span class="p">).</span><span class="k">await</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>Essentially, we would have to make <code>some_endpoint</code> available for both
asynchronous and blocking users. The important question here is, how do you do
this once you have dozens of endpoints? And how can you make it easy to switch
between async and sync for the user?</p>
</div>
<div class="sect2">
<h3 id="_good_ol_copy_pasting">Good ol&#39; copy-pasting</h3>
<div class="paragraph">
<p>This is what was first implemented. It was quite simple and it worked. You just
need to copy the regular client code into a new
<a href="https://github.com/ramsayleung/rspotify/tree/v0.9/src/blocking"><code>blocking</code> module
in Rspotify</a>. <a href="https://docs.rs/reqwest"><code>reqwest</code></a> (our HTTP client) and
<a href="https://docs.rs/reqwest/latest/reqwest/blocking/index.html"><code>reqwest::blocking</code></a>
share the same interface, so we can manually remove keywords like <code>async</code> or
<code>.await</code> and import <code>reqwest::blocking</code> instead of <code>reqwest</code> in the new module.</p>
</div>
<div class="paragraph">
<p>Then, the Rspotify user just can just use <code>rspotify::blocking::Client</code> instead
of <code>rspotify::Client</code>, and voilà! Their code is now blocking. This will bloat
the binary size for async-only users, so we can just feature-gate it under the
name <code>blocking</code> and done.</p>
</div>
<div class="paragraph">
<p>The problem was much more clear later on, though. Half the crate’s code was
duplicated. Adding a new endpoint or modifying it meant writing or removing
everything twice.</p>
</div>
<div class="paragraph">
<p>There is no way to make sure both implementations are equivalent unless you test
absolutely everything. Which isn’t a bad idea either, but maybe you copy-pasted
the tests wrong! How about that? The poor reviewer would have to read through
the same code twice to make sure both sides look alright — which sounds
incredibly prone to human errors.</p>
</div>
<div class="paragraph">
<p>In our experience, it really slowed down the development of Rspotify, specially
for new contributors who weren’t used to this whole ordeal. As a new excited
maintainer of Rspotify, I began to
<a href="https://github.com/ramsayleung/rspotify/issues/112">investigate other possible
solutions</a>.</p>
</div>
</div>
<div class="sect2">
<h3 id="_calling_block_on">Calling <code>block_on</code></h3>
<div class="paragraph">
<p><a href="https://github.com/ramsayleung/rspotify/pull/120">The second approach</a> consisted
on implementing everything on the async side. Then, you just make wrappers for
the blocking interface, which call
<a href="https://docs.rs/tokio/latest/tokio/runtime/struct.Runtime.html#method.block_on"><code>block_on</code></a>
internally. <code>block_on</code> will run the future until completion, basically
making it synchronous. You still need to copy the method <em>definitions</em>, but the
implementation is written once only:</p>
</div>
<div class="paragraph">
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="k">mod</span> <span class="nn">blocking</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">struct</span> <span class="nc">Spotify</span><span class="p">(</span><span class="k">super</span>::<span class="n">Spotify</span><span class="p">);</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">impl</span><span class="w"> </span><span class="n">Spotify</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="k">fn</span> <span class="nf">endpoint</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">param</span>: <span class="nb">String</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nc">SpotifyResult</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="n">runtime</span><span class="p">.</span><span class="n">block_on</span><span class="p">(</span><span class="k">async</span><span class="w"> </span><span class="k">move</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">                </span><span class="bp">self</span><span class="p">.</span><span class="mf">0.</span><span class="n">endpoint</span><span class="p">(</span><span class="n">param</span><span class="p">).</span><span class="k">await</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">            </span><span class="p">})</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>Note that in order to call <code>block_on</code>, you first have to create some kind of
runtime in the endpoint method. For example, with <a href="https://crates.io/crates/tokio"><code>tokio</code></a>
:</p>
</div>
<div class="paragraph">
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="kd">let</span><span class="w"> </span><span class="k">mut</span><span class="w"> </span><span class="n">runtime</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">tokio</span>::<span class="n">runtime</span>::<span class="n">Builder</span>::<span class="n">new</span><span class="p">()</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">.</span><span class="n">basic_scheduler</span><span class="p">()</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">.</span><span class="n">enable_all</span><span class="p">()</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">.</span><span class="n">build</span><span class="p">()</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">.</span><span class="n">unwrap</span><span class="p">();</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>This raises the question: should we initialize the runtime in each call to the
endpoint, or is there a way to share it? We could keep it as a global (<em>ewwww</em>),
or perhaps better, we can save the runtime in the <code>Spotify</code> struct. But since it
takes a <em>mutable</em> reference to the runtime, you’d have to wrap it up with
<code>Arc&lt;Mutex&lt;T&gt;&gt;</code>, completely killing the concurrency in your client. The proper
way to do this is with Tokio’s
<a href="https://docs.rs/tokio/latest/tokio/runtime/struct.Handle.html"><code>Handle</code></a>, which
looks like this:</p>
</div>
<div class="paragraph">
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="k">use</span><span class="w"> </span><span class="n">tokio</span>::<span class="n">runtime</span>::<span class="n">Runtime</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="fm">lazy_static!</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="c1">// You can also use `once_cell`
</span></span></span><span class="line"><span class="cl"><span class="c1"></span><span class="w">    </span><span class="k">static</span><span class="w"> </span><span class="k">ref</span><span class="w"> </span><span class="no">RT</span>: <span class="nc">Runtime</span><span class="w"> </span><span class="o">=</span><span class="w"> </span><span class="n">Runtime</span>::<span class="n">new</span><span class="p">().</span><span class="n">unwrap</span><span class="p">();</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">fn</span> <span class="nf">endpoint</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">,</span><span class="w"> </span><span class="n">param</span>: <span class="nb">String</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nc">SpotifyResult</span><span class="o">&lt;</span><span class="nb">String</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="no">RT</span><span class="p">.</span><span class="n">handle</span><span class="p">().</span><span class="n">block_on</span><span class="p">(</span><span class="k">async</span><span class="w"> </span><span class="k">move</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">        </span><span class="bp">self</span><span class="p">.</span><span class="mf">0.</span><span class="n">endpoint</span><span class="p">(</span><span class="n">param</span><span class="p">).</span><span class="k">await</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="p">})</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>While the handle does make our blocking client faster <a href="#block-on-perf">[1]</a>, there
is an even more performant way to do it. This is what <a href="https://crates.io/crates/reqwest"><code>reqwest</code></a>

itself does, in case you’re interested. In short, it spawns a thread that calls
<code>block_on</code> waiting on a channel with jobs <a href="#block-on-channels">[2]</a>
<a href="#block-on-reqwest">[3]</a>.</p>
</div>
<div class="paragraph">
<p>Unfortunately, this solution still has quite the overhead. You pull in large
dependencies like <code>futures</code> or <code>tokio</code>, and include them in your binary. All of
that, in order to…​ actually end up writing blocking code. So not only is it a
cost at runtime, but also at compile time. It just feels wrong to me.</p>
</div>
<div class="paragraph">
<p>And you still have a good amount of duplicate code, even if it’s just
definitions, which can sum up. <a href="https://crates.io/crates/reqwest"><code>reqwest</code></a>
 is a huge project and can
probably afford this for their <code>blocking</code> module. But for a less popular crate
like <code>rspotify</code>, this is harder to pull off.</p>
</div>
</div>
<div class="sect2">
<h3 id="_duplicating_the_crate">Duplicating the crate</h3>
<div class="paragraph">
<p>Another possible way to fix this is, as the features docs suggest, creating
separate crates. We’d have <code>rspotify-sync</code> and <code>rspotify-async</code>, and users would
just pick whichever crate they want as a dependency, even both if they need to.
The problem is — again — how exactly do we generate both versions of the
crate? <a href="https://github.com/ramsayleung/rspotify/pull/253">I was unable to do this
without copy-pasting the entire crate</a>, even with Cargo tricks like two
<code>Cargo.toml</code> files, one for each crate (which was quite inconvenient anyway).</p>
</div>
<div class="paragraph">
<p>With this idea we can’t even use procedural macros because you can’t just create
a new crate within a macro. We could define a file format to write templates of
Rust code in order to replace parts of the code like <code>async</code>/<code>.await</code>. But that
sounds completely out of scope.</p>
</div>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_what_ended_up_working_the_maybe_async_crate">What ended up “working”: the <code>maybe_async</code> crate</h2>
<div class="sectionbody">
<div class="paragraph">
<p><a href="https://github.com/ramsayleung/rspotify/pull/129">The third attempt</a> is based on
a crate called <a href="https://crates.io/crates/maybe_async"><code>maybe_async</code></a>
. I remember foolishly thinking it was
the perfect solution back when I discovered it.</p>
</div>
<div class="paragraph">
<p>Anyway, the idea is that with this crate you can automatically remove the
<code>async</code> and <code>.await</code> occurrences in your code with a procedural macro,
essentially automating the copy-pasting approach. For example:</p>
</div>
<div class="paragraph">
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="cp">#[maybe_async::maybe_async]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">async</span><span class="w"> </span><span class="k">fn</span> <span class="nf">endpoint</span><span class="p">()</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="cm">/* stuff */</span><span class="w"> </span><span class="p">}</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>Generates the following code:</p>
</div>
<div class="paragraph">
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="cp">#[cfg(not(feature = </span><span class="s">&#34;is_sync&#34;</span><span class="cp">))]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">async</span><span class="w"> </span><span class="k">fn</span> <span class="nf">endpoint</span><span class="p">()</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="cm">/* stuff */</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="cp">#[cfg(feature = </span><span class="s">&#34;is_sync&#34;</span><span class="cp">)]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">fn</span> <span class="nf">endpoint</span><span class="p">()</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="cm">/* stuff with `.await` removed */</span><span class="w"> </span><span class="p">}</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>You can configure whether you want asynchronous or blocking code by toggling the
<code>maybe_async/is_sync</code> feature when compiling the crate. The macro works for
functions, traits and <code>impl</code> blocks. If one conversion isn’t as easy as removing
<code>async</code> and <code>.await</code>, you can specify custom implementations with the
<code>async_impl</code> and <code>sync_impl</code> procedural macros. It does this wonderfully, and
we’ve already been using it for Rspotify for a while now.</p>
</div>
<div class="paragraph">
<p>In fact, it worked so well that I made Rspotify <em>http-client agnostic</em>, which is
even more flexible than being <em>async/sync agnostic</em>. This allows us to support
multiple HTTP clients like <a href="https://crates.io/crates/reqwest"><code>reqwest</code></a>
 and <a href="https://crates.io/crates/ureq"><code>ureq</code></a>
,
independently of whether the client is asynchronous or synchronous.</p>
</div>
<div class="paragraph">
<p>Being <em>http-client agnostic</em> is not that hard to implement if you have
<code>maybe_async</code> around. You just need to define a trait for the
<a href="https://github.com/ramsayleung/rspotify/blob/89b37219a2230cdcf08c4cfd2ebe46d64902f03d/rspotify-http/src/common.rs#L46">HTTP
client</a>, and then implement it for each of the clients you want to support:</p>
</div>
<div class="paragraph">
<div class="title">A snippet of code is worth a thousand words. (<em>You can find the full source for Rspotify’s <a href="https://github.com/ramsayleung/rspotify/blob/master/rspotify-http/src/reqwest.rs#L97"><code>reqwest</code>&#39;s client here</a>, and <a href="https://github.com/ramsayleung/rspotify/blob/master/rspotify-http/src/ureq.rs#L56"><code>ureq</code>&#39;s here</a></em>)</div>
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="cp">#[maybe_async]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">trait</span><span class="w"> </span><span class="n">HttpClient</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">async</span><span class="w"> </span><span class="k">fn</span> <span class="nf">get</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nb">String</span><span class="p">;</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="cp">#[sync_impl]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">impl</span><span class="w"> </span><span class="n">HttpClient</span><span class="w"> </span><span class="k">for</span><span class="w"> </span><span class="n">UreqClient</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">fn</span> <span class="nf">get</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nb">String</span> <span class="p">{</span><span class="w"> </span><span class="n">ureq</span>::<span class="n">get</span><span class="p">(</span><span class="cm">/* ... */</span><span class="p">)</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="cp">#[async_impl]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">impl</span><span class="w"> </span><span class="n">HttpClient</span><span class="w"> </span><span class="k">for</span><span class="w"> </span><span class="n">ReqwestClient</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">async</span><span class="w"> </span><span class="k">fn</span> <span class="nf">get</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">)</span><span class="w"> </span>-&gt; <span class="nb">String</span> <span class="p">{</span><span class="w"> </span><span class="n">reqwest</span>::<span class="n">get</span><span class="p">(</span><span class="cm">/* ... */</span><span class="p">).</span><span class="k">await</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">struct</span> <span class="nc">SpotifyClient</span><span class="o">&lt;</span><span class="n">Http</span>: <span class="nc">HttpClient</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="n">http</span>: <span class="nc">Http</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="cp">#[maybe_async]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">impl</span><span class="o">&lt;</span><span class="n">Http</span>: <span class="nc">HttpClient</span><span class="o">&gt;</span><span class="w"> </span><span class="n">SpotifyClient</span><span class="o">&lt;</span><span class="n">Http</span><span class="o">&gt;</span><span class="w"> </span><span class="p">{</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">    </span><span class="k">async</span><span class="w"> </span><span class="k">fn</span> <span class="nf">endpoint</span><span class="p">(</span><span class="o">&amp;</span><span class="bp">self</span><span class="p">)</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="bp">self</span><span class="p">.</span><span class="n">http</span><span class="p">.</span><span class="n">get</span><span class="p">(</span><span class="cm">/* ... */</span><span class="p">)</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="p">}</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>Then, we could extend it so that whichever client they want to use can be
enabled with feature flags in their <code>Cargo.toml</code>. For example, if <code>client-ureq</code>
is enabled, since <code>ureq</code> is synchronous, it would enable <code>maybe_async/is_sync</code>.
In turn, this would remove the <code>async</code>/<code>.await</code> and the <code>#[async_impl]</code> blocks,
and the Rspotify client would use <code>ureq</code>&#39;s implementation internally.</p>
</div>
<div class="paragraph">
<p>This solution has none of the downsides I listed in previous attempts:</p>
</div>
<div class="ulist">
<ul>
<li>
<p>No code duplication at all</p>
</li>
<li>
<p>No overhead neither at runtime nor at compile time. If the user wants a
blocking client, they can use <code>ureq</code>, which doesn’t pull <code>tokio</code> and friends</p>
</li>
<li>
<p>Quite easy to understand for the user; just configure a flag in you
<code>Cargo.toml</code></p>
</li>
</ul>
</div>
<div class="paragraph">
<p>However, stop reading for a couple of minutes and try to figure out why you
shouldn’t do this. In fact, I’ll give you 9 months, which is how long it took me
to do so…​</p>
</div>
<div class="sect2">
<h3 id="_the_problem">The problem</h3>
<div class="imageblock">
<div class="content">
<img src="/blog/rust-async-sync/preview.jpg" alt="preview" width="100%"/>
</div>
</div>
<div class="paragraph">
<p>Well, the thing is that features in Rust must be <strong>additive</strong>: “enabling a
feature should not disable functionality, and it should usually be safe to
enable any combination of features”. Cargo may merge features of a crate when
it’s duplicated in the dependency tree in order to avoid compiling the same
crate multiple times.
<a href="https://doc.rust-lang.org/cargo/reference/features.html#feature-unification">The
reference explains this quite well, if you want more details</a>.</p>
</div>
<div class="paragraph">
<p>This optimization means that mutually exclusive features may break a dependency
tree. In our case, <code>maybe_async/is_sync</code> is a <em>toggle</em> feature enabled by
<code>client-ureq</code>. So if you try to compile it with <code>client-reqwest</code> also enabled,
it will fail because <code>maybe_async</code> will be configured to generate synchronous
function signatures instead. It’s impossible to have a crate that depends on
both sync and async Rspotify either directly or indirectly, and the whole
concept of <code>maybe_async</code> is currently wrong according to the Cargo reference.</p>
</div>
</div>
<div class="sect2">
<h3 id="_the_feature_resolver_v2">The feature resolver v2</h3>
<div class="paragraph">
<p>A common misconception is that this is fixed by the “feature resolver v2”,
which
<a href="https://doc.rust-lang.org/cargo/reference/features.html#feature-resolver-version-2">the
reference also explains quite well</a>. It has been enabled by default since the
2021 edition, but you can specify it inside your <code>Cargo.toml</code> in previous ones.
This new version, among other things, avoids unifying features in some special
cases, but not in ours:</p>
</div>
<div class="quoteblock">
<blockquote>
<div class="ulist">
<ul>
<li>
<p>Features enabled on platform-specific dependencies for targets not currently
being built are ignored.</p>
</li>
<li>
<p>Build-dependencies and proc-macros do not share features with normal
dependencies.</p>
</li>
<li>
<p>Dev-dependencies do not activate features unless building a target that needs
them (like tests or examples).</p>
</li>
</ul>
</div>
</blockquote>
</div>
<div class="paragraph">
<p>Just in case, I tried to reproduce this myself, and it did work as I expected.
<a href="https://github.com/marioortizmanero/resolver-v2-conflict">This repository</a> is an
example of conflicting features, which breaks with any feature resolver.</p>
</div>
</div>
<div class="sect2">
<h3 id="_other_fails">Other fails</h3>
<div class="paragraph">
<p>There were a few crates that also had this problem:</p>
</div>
<div class="ulist">
<ul>
<li>
<p><a href="https://crates.io/crates/arangors"><code>arangors</code></a>
 and <a href="https://crates.io/crates/aragog"><code>aragog</code></a>
: wrappers for ArangoDB. Both
use <code>maybe_async</code> to switch between async and sync (<code>arangors</code>&#39;s author is
the same person, in fact) <a href="#arangors-error">[5]</a> <a href="#aragog-error">[6]</a>.</p>
</li>
<li>
<p><a href="https://crates.io/crates/inkwell"><code>inkwell</code></a>
: a wrapper for LLVM. It supports multiple versions of
LLVM, which are not compatible with eachother <a href="#inkwell-error">[7]</a>.</p>
</li>
<li>
<p><a href="https://crates.io/crates/k8s-openapi"><code>k8s-openapi</code></a>
: a wrapper for Kubernetes, with the same issue as
<code>inkwell</code> <a href="#k8s-error">[8]</a>.</p>
</li>
</ul>
</div>
</div>
<div class="sect2">
<h3 id="_fixing_maybe_async">Fixing <code>maybe_async</code></h3>
<div class="paragraph">
<p>Once the crate started to gain popularity, this issue was opened in
<code>maybe_async</code>, which explains the situation and showcases a fix:</p>
</div>
<div class="paragraph text-center">








  



<p class="gh-reference">

  
  
    <svg class="gh-icon" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16"><path d="M8 9.5a1.5 1.5 0 100-3 1.5 1.5 0 000 3z"/><path fill-rule="evenodd" d="M8 0a8 8 0 100 16A8 8 0 008 0zM1.5 8a6.5 6.5 0 1113 0 6.5 6.5 0 01-13 0z"/></svg>
  

  <a class="gh-title" href="https://github.com/fMeow/maybe-async-rs/issues/6" style="text-decoration: none;">async and sync in the same program
  
    <span class="gh-number">fMeow/maybe-async-rs#6</span></a>
  

</p>





</div>
<div class="paragraph">
<p><code>maybe_async</code> would now have two feature flags: <code>is_sync</code> and <code>is_async</code>. The
crate would generate the functions in the same way, but with a <code>_sync</code> or
<code>_async</code> suffix appended to the identifier so that they wouldn’t be conflicting.
For example:</p>
</div>
<div class="paragraph">
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="cp">#[maybe_async::maybe_async]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">async</span><span class="w"> </span><span class="k">fn</span> <span class="nf">endpoint</span><span class="p">()</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="cm">/* stuff */</span><span class="w"> </span><span class="p">}</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>Would now generate the following code:</p>
</div>
<div class="paragraph">
<div class="highlight"><pre tabindex="0" class="chroma"><code class="language-rust" data-lang="rust"><span class="line"><span class="cl"><span class="cp">#[cfg(feature = </span><span class="s">&#34;is_async&#34;</span><span class="cp">)]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">async</span><span class="w"> </span><span class="k">fn</span> <span class="nf">endpoint_async</span><span class="p">()</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="cm">/* stuff */</span><span class="w"> </span><span class="p">}</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="cp">#[cfg(feature = </span><span class="s">&#34;is_sync&#34;</span><span class="cp">)]</span><span class="w">
</span></span></span><span class="line"><span class="cl"><span class="w"></span><span class="k">fn</span> <span class="nf">endpoint_sync</span><span class="p">()</span><span class="w"> </span><span class="p">{</span><span class="w"> </span><span class="cm">/* stuff with `.await` removed */</span><span class="w"> </span><span class="p">}</span></span></span></code></pre></div>
</div>
<div class="paragraph">
<p>However, these suffixes introduce noise, so I wondered if it would be possible
to do it in a more ergonomic way. I forked <code>maybe_async</code> and gave it a try,
about which you can read more
<a href="https://github.com/fMeow/maybe-async-rs/issues/6#issuecomment-880581551">in this
series of comments</a>. In summary, it was too complicated, and I ultimately gave
up.</p>
</div>
<div class="paragraph">
<p>The only way to fix this edge case would be to worsen the usability of Rspotify
for everyone. But I’d argue that someone who depends on both async and sync is
unlikely; we haven’t actually had anyone complaining yet. Unlike <code>reqwest</code>,
<code>rspotify</code> is a “high level” library, so it’s hard to imagine a scenario where
it appears more than once in a dependency tree in the first place.</p>
</div>
<div class="paragraph">
<p>Perhaps we could ask the Cargo devs for help?</p>
</div>
</div>
<div class="sect2">
<h3 id="_official_support">Official Support</h3>
<div class="paragraph">
<p>Rspotify is far from being the first who has been through this problem, so it
might be interesting to read previous discussions about it:</p>
</div>
<div class="ulist">
<ul>
<li>
<p><a href="https://github.com/rust-lang/rfcs/pull/2962">This now-closed RFC for the Rust
compiler</a> suggested adding the <code>oneof</code> configuration predicate (think
<code>#[cfg(any(…​))]</code> and similars) to support exclusive features. This only
makes it easier to have conflicting features for cases where there’s <em>no
choice</em>, but features should still be strictly additive.</p>
</li>
<li>
<p>The previous RFC started
<a href="https://internals.rust-lang.org/t/pre-rfc-cargo-mutually-exclusive-features/13182/27">some
discussion</a> in the context of allowing exclusive features in Cargo itself, and
although it has some interesting info, it didn’t go too far.</p>
</li>
<li>
<p><a href="https://github.com/rust-lang/cargo/issues/2980">This issue in Cargo</a> explains a
similar case with the Windows API. The discussion includes more examples and
solution ideas, but none have made it to Cargo yet.</p>
</li>
<li>
<p><a href="https://github.com/rust-lang/cargo/issues/4803">Another issue in Cargo</a> asks
for a way to test and build with combinations of flags easily. If features are
strictly additive, then <code>cargo test --all-features</code> will cover everything. But
in case it doesn’t, the user has to run the command with multiple combinations
of feature flags, which is quite cumbersome. This is already possible
unofficially thanks to <a href="https://github.com/taiki-e/cargo-hack"><code>cargo-hack</code></a>.</p>
</li>
<li>
<p>A completely different approach
<a href="https://blog.rust-lang.org/inside-rust/2023/02/23/keyword-generics-progress-report-feb-2023.html">based
on the Keyword Generics Initiative</a>. It seems to be the most recent take on
solving this, but it’s in an “exploration” phase, and
<a href="https://blog.rust-lang.org/inside-rust/2022/07/27/keyword-generics.html#q-is-there-an-rfc-available-to-read">no
RFCs are available as of this writing</a>.</p>
</li>
</ul>
</div>
<div class="paragraph">
<p>According to
<a href="https://github.com/rust-lang/rfcs/pull/2962#issuecomment-664656377">this old
comment</a>, it’s not something the Rust team has already discarded; it’s still
being discussed.</p>
</div>
<div class="paragraph">
<p>Although unofficial, another interesting approach that could be explored further
in Rust is <a href="https://sans-io.readthedocs.io/">“Sans I/O”</a>. This is a Python
protocol that abstracts away the use of network protocols like HTTP in our case,
thus maximizing reusability.</p>
</div>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_conclusion">Conclusion</h2>
<div class="sectionbody">
<div class="paragraph">
<p>We currently have a choice to make between:</p>
</div>
<div class="ulist">
<ul>
<li>
<p>Ignoring the Cargo Reference. We could assume that noone is going to use both
sync and async for Rspotify at the same time.</p>
</li>
<li>
<p>Fixing <code>maybe_async</code> and adding <code>_async</code> and <code>_sync</code> suffixes to each endpoint
in our library.</p>
</li>
<li>
<p>Dropping support for both async and sync code. It’s kind of become a mess that
we don’t have the manpower to deal with and that
<a href="https://github.com/ramsayleung/rspotify/pull/224#issuecomment-909324671">affects
other parts of Rspotify</a>. The problem is that some crates that depend on
rspotify like <a href="https://github.com/hrkfdn/ncspot"><code>ncspot</code></a> or
<a href="https://github.com/Spotifyd/spotifyd"><code>spotifyd</code></a> are blocking, and others like
<a href="https://github.com/Rigellute/spotify-tui"><code>spotify-tui</code></a> use async, so I’m not
sure what they’d think.</p>
<div class="paragraph">
<p>I know this is a problem that I’ve imposed to myself. We could just say “No. We
only support async” or “No. We only support sync”. While there are users
interested in being able to use both, sometimes you just have to say no. If such
a feature becomes so complicated to deal with that your entire codebase becomes
a mess, and you don’t have the engineering power to maintain it, then it’s your
only choice. If someone cared enough, they could just fork the crate and convert
it to synchronous for their own usage.</p>
</div>
<div class="paragraph">
<p>After all, most API wrappers and the like only support either asynchronous or
blocking code. <a href="https://crates.io/crates/serenity"><code>serenity</code></a>
 (Discord API), <a href="https://crates.io/crates/sqlx"><code>sqlx</code></a>
 (SQL
toolkit) and <a href="https://crates.io/crates/teloxide"><code>teloxide</code></a>
 (Telegram API) are async-only, for example,
and they’re quite popular.</p>
</div>
</li>
</ul>
</div>
<div class="paragraph">
<p>Even though it was quite frustrating at times, I don’t really regret spending so
much time walking in circles trying to get both async and sync to work. I was
contributing to Rspotify in the first place just to <em>learn</em>. I had no deadlines
and no stress, I just wanted to try to improve a library in Rust in my free
time. And I <em>have</em> learned a lot; hopefully you too, after reading this.</p>
</div>
<div class="paragraph">
<p>Perhaps the lesson today is that we should remember that Rust is a low level
language after all, and there are some things that aren’t possible without a lot
of complexity. Anyhow, I’m looking forward to how the Rust team fixes this in
the future.</p>
</div>
<div class="paragraph">
<p>So what do you think? What would you do if you were a maintainer of Rspotify?
You can leave a comment below if you like.</p>
</div>
</div>
</div>
<div class="sect1">
<h2 id="_references">References</h2>
<div class="sectionbody">
<div class="ulist bibliography">
<ul class="bibliography">
<li>
<p><a id="block-on-perf"></a>[1] 







  



<span class="gh-reference">

  
  
    <svg class="gh-icon" width="16" height="16" viewBox="0 0 20 20" version="1.1" id="svg1" xmlns="http://www.w3.org/2000/svg" xmlns:svg="http://www.w3.org/2000/svg"> <defs id="defs1" /> <path d="M 3.25,15 C 1.4550746,15 0,13.544925 0,11.75 V 3.25 C 0,1.4550746 1.4550746,0 3.25,0 h 13.5 C 18.544925,0 20,1.4550746 20,3.25 v 8.5 C 20,13.544925 18.544925,15 16.75,15 H 11.012485 L 5.9986815,18.750679 C 5.4458514,19.164165 4.6624979,19.051204 4.2490125,18.498373 4.0873576,18.282241 4,18.019591 4,17.749906 L 3.9992143,15 Z m 7.263515,-1.5 H 16.75 c 0.966498,0 1.75,-0.783502 1.75,-1.75 V 3.25 C 18.5,2.2835017 17.716498,1.5 16.75,1.5 H 3.25 C 2.2835017,1.5 1.5,2.2835017 1.5,3.25 v 8.5 c 0,0.966498 0.7835017,1.75 1.75,1.75 H 5.4987857 L 5.499,14.249786 5.4998574,17.25057 Z" id="path1" /> </svg>
  

  <a class="gh-title" href="https://github.com/ramsayleung/rspotify/issues/112#issuecomment-683266508" style="text-decoration: none;">Cleaning up the <code>blocking</code> module
  
    
    <span class="gh-number">ramsayleung/rspotify#112 <i>(comment)</i></span></a>
  

</span>




</p>
</li>
<li>
<p><a id="block-on-channels"></a>[2] <a href="https://github.com/seanmonstar/reqwest/blob/0.10.x/src/blocking/client.rs#L757">reqwest/src/blocking/client.rs @ line 757 — GitHub</a></p>
</li>
<li>
<p><a id="block-on-reqwest"></a>[3] 







  



<span class="gh-reference">

  
  
    <svg class="gh-icon" width="16" height="16" viewBox="0 0 20 20" version="1.1" id="svg1" xmlns="http://www.w3.org/2000/svg" xmlns:svg="http://www.w3.org/2000/svg"> <defs id="defs1" /> <path d="M 3.25,15 C 1.4550746,15 0,13.544925 0,11.75 V 3.25 C 0,1.4550746 1.4550746,0 3.25,0 h 13.5 C 18.544925,0 20,1.4550746 20,3.25 v 8.5 C 20,13.544925 18.544925,15 16.75,15 H 11.012485 L 5.9986815,18.750679 C 5.4458514,19.164165 4.6624979,19.051204 4.2490125,18.498373 4.0873576,18.282241 4,18.019591 4,17.749906 L 3.9992143,15 Z m 7.263515,-1.5 H 16.75 c 0.966498,0 1.75,-0.783502 1.75,-1.75 V 3.25 C 18.5,2.2835017 17.716498,1.5 16.75,1.5 H 3.25 C 2.2835017,1.5 1.5,2.2835017 1.5,3.25 v 8.5 c 0,0.966498 0.7835017,1.75 1.75,1.75 H 5.4987857 L 5.499,14.249786 5.4998574,17.25057 Z" id="path1" /> </svg>
  

  <a class="gh-title" href="https://github.com/ramsayleung/rspotify/issues/112#issuecomment-683249563" style="text-decoration: none;">Cleaning up the <code>blocking</code> module
  
    
    <span class="gh-number">ramsayleung/rspotify#112 <i>(comment)</i></span></a>
  

</span>




</p>
</li>
<li>
<p><a id="features-additive"></a>[4] <a href="https://github.com/rust-lang/cargo/blob/master/src/doc/src/reference/features.md#feature-unification">Cargo’s Documentation, “Feature unification”</a></p>
</li>
<li>
<p><a id="arangors-error"></a>[5] 







  



<span class="gh-reference">

  
  
    <svg class="gh-icon" xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 16 16"><path d="M8 9.5a1.5 1.5 0 100-3 1.5 1.5 0 000 3z"/><path fill-rule="evenodd" d="M8 0a8 8 0 100 16A8 8 0 008 0zM1.5 8a6.5 6.5 0 1113 0 6.5 6.5 0 01-13 0z"/></svg>
  

  <a class="gh-title" href="https://github.com/fMeow/arangors/issues/37" style="text-decoration: none;">Proposal: Move <code>sync</code> and <code>async</code> features into seperate modules
  
    <span class="gh-number">fMeow/arangors#37</span></a>
  

</span>




</p>
</li>
<li>
<p><a id="aragog-error"></a>[6] <a href="https://gitlab.com/qonfucius/aragog/-/blob/0.140.0/src/lib.rs#L488">aragog/src/lib.rs @ line 488 — GitLab</a></p>
</li>
<li>
<p><a id="inkwell-error"></a>[7] <a href="https://github.com/TheDan64/inkwell/blob/bfb0e32bc329fd35f6c5a529a1a6209936a147f8/src/lib.rs#L107">inkwell/src/lib.rs @ line 107 — GitHub</a></p>
</li>
<li>
<p><a id="k8s-error"></a>[8] <a href="https://github.com/Arnavion/k8s-openapi/blob/v0.13.0/build.rs#L31">k8s-openapi/build.rs @ line 31 — GitHub</a></p>
</li>
</ul>
</div>
</div>
</div>
]]></content:encoded>
    </item>
  </channel>
</rss>
